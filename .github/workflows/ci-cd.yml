name: CI-CD Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ docker login -u abhishek231805 }}
          password: ${{ dckr_pat_U92Zg3ptT9E3-rJHgGv37m92T_AD }}

      - name: Build & Push Backend
        run: |
          docker build -t ${{ docker login -u abhishek231805 }}/django-backend ./backend
          docker push ${{ docker login -u abhishek231805}}/django-backend

      - name: Build & Push Frontend
        run: |
          docker build -t ${{ docker login -u abhishek231805 }}/react-frontend ./frontend
          docker push ${{ docker login -u abhishek231805 }}/react-frontend
